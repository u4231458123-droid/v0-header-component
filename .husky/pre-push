#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# =============================================================================
# MyDispatch - Pre-Push Hook
# =============================================================================
# Basierend auf CPO-Vorgaben
# =============================================================================

echo "🔍 Pre-Push Validierung..."
echo -e "==========================\n"

# =============================================================================
# PHASE 1: BUILD TEST
# =============================================================================

echo "🏗️ Phase 1: Build Test..."
npm run build || {
  echo -e "\n❌ Build fehlgeschlagen - Push abgebrochen"
  exit 1
}

# =============================================================================
# PHASE 2: ABHÄNGIGKEITS-PRÜFUNG
# =============================================================================

echo -e "\n🔗 Phase 2: Abhängigkeits-Prüfung..."
if [ -f "scripts/cicd/check-dependencies.mjs" ]; then
  node scripts/cicd/check-dependencies.mjs
  EXIT_CODE=$?
  if [ $EXIT_CODE -eq 0 ]; then
    echo -e "\n✅ Abhängigkeits-Prüfung erfolgreich"
  else
    # Alle Exit-Codes außer 0 werden als Fehler behandelt
    echo -e "\n❌ Abhängigkeits-Prüfung fehlgeschlagen (Exit-Code: $EXIT_CODE) - Push abgebrochen"
    exit 1
  fi
else
  echo "⚠️ Abhängigkeits-Prüfungs-Script nicht gefunden - überspringe"
fi

echo -e "\n✅ Pre-Push Validierung erfolgreich!"
